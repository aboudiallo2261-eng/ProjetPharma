<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE hibernate-configuration PUBLIC
        "-//Hibernate/Hibernate Configuration DTD 3.0//EN"
        "http://www.hibernate.org/dtd/hibernate-configuration-3.0.dtd">
<hibernate-configuration>
    <session-factory>
        <!-- Database connection settings -->
        <!-- SÉCURITÉ : url, username et password sont injectés au démarrage par HibernateUtil
             depuis config.properties (fichier local NON versionné — voir config.properties.example). -->
        <property name="connection.driver_class">com.mysql.cj.jdbc.Driver</property>

        <!-- JDBC connection pool (use the built-in) -->
        <property name="connection.pool_size">10</property>

        <!-- SQL dialect -->
        <property name="dialect">org.hibernate.dialect.MySQLDialect</property>

        <!-- Désactivé en production pour la sécurité et les performances (activer pour debug uniquement) -->
        <property name="show_sql">false</property>
        <property name="format_sql">false</property>

        <!-- Drop and re-create the database schema on startup for early development (change to update later) -->
        <property name="hbm2ddl.auto">update</property>

        <!-- List of mapped classes will be added here -->
    </session-factory>
</hibernate-configuration>
